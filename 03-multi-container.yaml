apiVersion: v1
kind: Pod
metadata:
  name: multi-container-pod
  labels:
    project: roboshop
    environment: dev
    purpose: poc
spec:
  containers:
    - name: nginx
      image: nginx:latest
    - name: almalinux
      image: almalinux:9

